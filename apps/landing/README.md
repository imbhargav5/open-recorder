# Open Recorder Landing

Next.js landing page for Open Recorder.

Run locally:

```bash
pnpm install --dir apps/landing --frozen-lockfile
pnpm --dir apps/landing dev
```

Validate:

```bash
pnpm --dir apps/landing lint
pnpm --dir apps/landing build
```
